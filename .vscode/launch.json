{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Run toolbox (Node + sqlite)",
      "type": "node",
      "request": "launch",
      "program": "${workspaceFolder}/server/server.js",
      "args": ["--experimental-sqlite"],
      "console": "integratedTerminal",
      "env": {
        "PORT": "4173"
      }
    }
  ]
}
